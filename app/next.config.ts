import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // SST's Nextjs component handles the output configuration automatically.
  // Do not set `output: "export"` — that disables SSR and API routes.
};

export default nextConfig;
