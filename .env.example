# Root .env — read by SST infra code only, not by Next.js.
# Copy to .env for local development.

# Set to "true" to deploy the async job queue and WebSocket notification API.
# See CLAUDE.md → "Async jobs" for the full workflow.
# Without this flag no queue or WebSocket resources are created.
ENABLE_ASYNC_JOBS=
